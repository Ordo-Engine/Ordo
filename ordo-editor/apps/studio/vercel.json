{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "curl https://sh.rustup.rs -sSf | sh -s -- -y --target wasm32-unknown-unknown && export PATH=\"${CARGO_HOME:-$HOME/.cargo}/bin:$PATH\" && cargo install wasm-pack --locked && cd ../.. && pnpm --filter @ordo-engine/studio build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "https://api.ordoengine.com/api/v1/:path*"
    }
  ]
}
